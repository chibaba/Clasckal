import React, { Component } from "react";
import "./myStyle.css";

export class Navbar extends Component {
    render() {
        return (
            <nav className="nav-bar">
                <h1>Most Profound Choice Maker App</h1>
            </nav>
        );
    }
}

export default Navbar;
